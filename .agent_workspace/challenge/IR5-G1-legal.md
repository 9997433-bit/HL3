# IR5-G1 法律（父调度器代行）

- `src/` 无 iris / vic-snap / keygen / OpenCorr 源码 / GPU .lib。
- `download("sem")` 拒绝。manifest 登记 SEM Drive id 为 denied。
- `git status` 不得出现 `cache/**/*.tif`。MATLAB 未入库。
- `crack`：`tests/test_legal_scan.py` 自拼正则；`icgn.py` 已去掉 crack tip 用词。
