# IR5-F2 Stereo Challenge 协议（父调度器代行）

官方 Drive：`1q0GnsopWaOlD6IuVOS-ZxeBIZwUFuQ-w`。Sample 1 实验图：`1QRWQZEDuTbjqR4d_znW0TNXBNYo2Y-F0`。
`Translate.zip`（1.06 GB）含 16-mm / 35-mm 台步 TIFF（`_0` 左 / `_1` 右）与 `AeroStage.csv`。

标定：同目录有各厂商靶标 zip 与 `CMMData_mm.csv`，**没有**现成 3×4 `P` 文本。要出 3D RMS 必须 Zhang+Brown 或解析厂商标定文件。本轮未做 Zhang。

因此本轮只允许：左相机 2D 诊断 + 明确声明不是 Ahmad et al. 2024 的 3D 成绩。论文 <80 µm / ±20 mm 在无畸变针孔、无标定下不可比。
